# Postgres / PostGIS (used by docker-compose.yml)
POSTGRES_USER=urbansdk
POSTGRES_PASSWORD=urbansdk
POSTGRES_DB=urbansdk
POSTGRES_PORT=5432
ADMINER_PORT=8080

# Used by the FastAPI app / ingestion script
DATABASE_URL=postgresql+asyncpg://urbansdk:urbansdk@localhost:5432/urbansdk

# Only needed for the notebook (Step 6) — get a free token at mapbox.com
MAPBOX_TOKEN=
